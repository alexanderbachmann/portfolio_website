import { BookOpen, Github, Linkedin } from 'lucide-react';
import KaggleIcon from './KaggleIcon';

/* Keyed by the `label` of each entry in src/data/site.js `socials`. */
export const socialIconByLabel = {
  LinkedIn: Linkedin,
  GitHub: Github,
  Kaggle: KaggleIcon,
  GoodReads: BookOpen,
};
